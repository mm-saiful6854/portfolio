services:
  portfolio:
    image: ${PORTFOLIO_IMAGE:mmsaiful6854/portfolio:latest}
    container_name: portfolio
    restart: unless-stopped
    pull_policy: always
    ports:
      - "${PORT:-80}:80"
